{
  "name": "multishop-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "clean": "rimraf ../public",
    "prebuild": "npm run clean",
    "build": "npm run prebuild && vite build",
    "preview": "vite preview --port 5173"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.4",
    "rimraf": "^6.1.3",
    "vite": "^7.3.1"
  }
}
